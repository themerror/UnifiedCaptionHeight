<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="UnifiedCaptionHeight.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:UnifiedCaptionHeight"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	mc:Ignorable="d">
	<Grid HorizontalAlignment="Center" VerticalAlignment="Center">
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<TextBlock
			Grid.Row="0"
			Grid.Column="0"
			VerticalAlignment="Center">
			Your Dpi is:
		</TextBlock>
		<TextBox
			Name="displayDpi"
			Grid.Row="0"
			Grid.Column="1"
			Margin="5,0,0,0"
			IsReadOnly="True" />
		<Button
			Grid.Row="2"
			Grid.Column="1"
			Margin="5,10,0,0"
			Click="Button_Click">
			Go!
		</Button>
		<Button
			Grid.Row="2"
			Grid.Column="0"
			Margin="5,10,0,0"
			Click="Button_Click_1">
			Reset
		</Button>
	</Grid>
</Page>